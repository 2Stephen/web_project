import Vue, { PropOptions } from 'vue';
import { AMapTarget } from './amap-vue';

export declare interface PropDesc<T, U extends AMapTarget = AMapTarget>
  extends PropOptions<T> {
  setter?: ((this: Vue, component: U, value: T) => void) | string;
  getter?: ((this: Vue, component: U, e: unknown) => T) | string;
  deep?: boolean;
  equals?(a: T, b: T): boolean;
  clone?: boolean;
  events?: string[];
  transform?: (this: Vue, value: T) => any;
  initialize?: boolean;
}
